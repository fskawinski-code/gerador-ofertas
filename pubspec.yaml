name: gerador_ofertas
description: App para gerar mensagens de afiliado
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
